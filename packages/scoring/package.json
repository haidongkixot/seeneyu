{
  "name": "@seeneyu/scoring",
  "version": "0.1.0",
  "private": true,
  "description": "Shared MediaPipe scoring primitives used by seeneyu web and the Mirror Mode extension.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
